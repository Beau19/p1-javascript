let checkInput = (x, y, out) => { if (x < 1 || y < 1) out.innerText = "het getal is te laag"; };
let multiply = (x, y, out) => out.innerText = x * y;
let subtract = (x, y, out) => out.innerText = x - y;
let add = (x, y, out) => out.innerText = x + y;
let divide = (x, y, out) => out.innerText = x / y;